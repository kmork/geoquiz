/**
 * Capitals - Inline Game (no separate main file exists)
 * Capitals game is embedded in capitals.html directly
 * This placeholder ensures Daily Challenge can import if needed
 */

// Capitals game doesn't have a separate main.js - it's inline in capitals.html
// For Daily Challenge, we continue using the existing approach
export function createCompleteCapitalsGame() {
  console.warn('Capitals game uses inline implementation');
  return null;
}
