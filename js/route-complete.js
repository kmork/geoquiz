/**
 * Connect the Countries (Route) - Inline Game
 * Route game is embedded in route.html directly
 * This placeholder ensures Daily Challenge can import if needed
 */

// Route game doesn't have separate factories - embedded in HTML
// For Daily Challenge, we continue using the existing approach
export function createCompleteRouteGame() {
  console.warn('Route game uses inline implementation');
  return null;
}
