/**
 * Heritage (UNESCO Sites) - Complete Game Factory
 * Used by both standalone and Daily Challenge
 */

import { createCompletePictureGame } from "./picture-complete.js";

// Export with new name
export { createCompletePictureGame as createCompleteHeritageGame };
